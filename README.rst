Bootstrap
---------
Requires:
 - Python >= 3.3;
 - pip >= 1.4 (with wheel support);

Steps:
 - create virtualenv and activate it;
 - run ``./bootsrap``.


Usage
-----
Use ``./pusto.py --help`` for information.
